- name: Criar OperatorGroup
  k8s:
    state: present
    definition: "{{ lookup('template', 'cert-operatorgroup.yaml') }}"

- name: Criar Subscription
  k8s:
    state: present
    definition: "{{ lookup('template', 'cert-subscription.yaml') }}"

#- name: Criar CertManager Custom Resource
#  k8s:
#    state: present
#    definition: "{{ lookup('template', 'cert-manager_cr.yaml') }}"
